# Design Inspiration

This folder is a temporary copy of UI inspiration.

- It is mounted at `/design-inspiration` via the route wrapper in `src/routes/design-inspiration.tsx`.
- The code here is intentionally co-located so it can be deleted later in one pass.
- Treat this folder as reference/inspiration code first, not core app architecture.

If we no longer need it, remove this folder and the `/design-inspiration` route/link.
